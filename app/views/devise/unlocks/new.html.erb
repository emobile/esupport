<div id="body">
  <div class="row-fluid">
    <div class="dialog">
      <div class="block">
        <a href="#<%=request.env['ORIGINAL_FULLPATH'].sub(/^(.*\/).*/, '\1/').sub('/unlock//', '').sub('/', '')%>" class="block-heading" data-toggle="collapse"><%="Resend unlock instructions"%><span class="label label-notice"><%=t(:help)%></span></a>
        <div class="block-body">
          <%= form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post }) do |f| %>
            <%= devise_error_messages! %>

            <div><%= f.label :email %>
              <%= f.email_field :email, :autofocus => true %></div>
            <br />
            <div><%= f.submit "Resend unlock instructions", :class=>"btn btn-primary btn-medium" %></div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
